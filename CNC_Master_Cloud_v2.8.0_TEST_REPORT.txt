CNC Master Cloud v2.8.0 — TEST REPORT
Дата сборки: 2026-07-29

БАЗА СБОРКИ
- Исходная рабочая версия: v2.7.0 Desktop UI Rebuild.
- Функциональная ветка: v2.6.0 AI Stock Removal.
- Тестовый файл: «проба болта.pdf».

ИСПРАВЛЕНИЯ
1. Выделенная область PDF сохраняется после «Распознать область» и сразу доступна кнопке AI-контур.
2. Возврат страницы действительно загружает исходную страницу.
3. Убрано ложное ограничение интерфейса по соотношению сторон выделения.
4. Почти плоская линия не получает HIGH и не применяется как AutoContour.
5. Текст и размеры PDF извлекаются только из выделенного фрагмента.
6. Исправлен анализ PNG/JPG: autocontour_diagnostics всегда определён.
7. AI-контур приводится к Z0 на переднем торце и отрицательному Z в глубину детали.
8. Пары точек с одинаковым Z сохраняются. Вертикальная ступень X90 -> X125 на Z-16 больше не исчезает.
9. Скрипт Desktop UI больше не меняет номер версии обратно на v2.7.0.
10. После распознавания область выходит из режима выделения и возвращается к редактированию.

АВТОМАТИЧЕСКИЕ ПРОВЕРКИ
- Python compileall: OK.
- JavaScript syntax, 2 inline-блока: OK (node --check).
- Pytest: 42 passed.

ПРОВЕРКА НА «ПРОБА БОЛТА.PDF»
- Полная страница: confidence=medium, 11 точек; причина: «слишком плоский кандидат; возможна размерная линия».
- Выделенный осевой разрез: confidence=medium, 18 точек; причина: «слишком плоский кандидат; возможна размерная линия».
- Узкий осевой разрез: confidence=low, кандидат отклонён.

ИТОГ AUTOCONTOUR
Ни один подозрительный плоский кандидат не получил HIGH, поэтому интерфейс не применит размерную линию как профиль автоматически. Для сложного заштрихованного осевого разреза предусмотрен AI-контур или ручное подтверждение.

ПРОВЕРКА СТУПЕНИ STOCK REMOVAL
Контрольный контур:
X90 Z0
X90 Z-16
X125 Z-16
X125 Z-33

Результат генератора сохранил все 4 точки, включая две точки на Z-16.

ОГРАНИЧЕНИЕ ТЕСТА
Живой запрос OpenAI не выполнялся, потому что в локальной тестовой среде нет OPENAI_API_KEY. Формирование запроса, валидация ответа, нормализация X/Z и обработка ошибок покрыты тестами. На Railway требуется задать OPENAI_API_KEY и при необходимости OPENAI_DRAWING_MODEL.

БЕЗОПАСНОСТЬ
Автоконтур, AI-контур и G-код являются проектными данными. Перед станком обязательны проверка размеров, X0/Z0, режима X, коррекций, симуляция и сухой прогон.
