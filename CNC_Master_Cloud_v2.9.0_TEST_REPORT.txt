CNC Master Cloud v2.9.0 — TEST REPORT
Build: 2026-07-30-manual-contour-stock-removal

РЕАЛИЗОВАНО
- Ручной маркер контура поверх PDF/фото или пустого поля.
- Маркеры создаются без OpenAI и без обязательной калибровки.
- Точные X/Z хранятся отдельно от положения маркеров на изображении.
- Редактирование и удаление любой точки в таблице.
- Сохранение вертикальных плеч: соседние точки с одинаковым Z не удаляются.
- Расчёт черновых проходов Stock Removal по Ø заготовки, ap и припуску.
- Оценка длины профиля, максимального радиального съёма и объёма снятия.
- Формирование графики, карты SINUMERIK 828D и проектного G-кода.
- AutoContour и OpenAI оставлены дополнительными режимами.

АВТОМАТИЧЕСКИЕ ПРОВЕРКИ
1. Python compileall: PASSED
2. Pytest: 45 PASSED
3. JavaScript Node syntax check: 2/2 PASSED
4. Статическая проверка DOM ID: PASSED
   - дубликатов ID: 0
   - отсутствующих элементов, вызванных через $(): 0
5. Headless Chromium E2E: PASSED
   - открыт интерфейс v2.9.0
   - поставлены 4 ручных маркера
   - введены X80 Z0; X80 Z-20; X60 Z-20; X60 Z-50
   - контур подтверждён
   - отправлен запрос /api/v1/client/generate
   - показаны 5 проходов и 123.4 см³ из тестового ответа
   - ошибок JavaScript/console: 0
6. Реальный модуль расчёта Python: PASSED
   - заготовка Ø100 × 60
   - контур X80 Z0; X80 Z-20; X60 Z-20; X60 Z-50
   - ap=2 мм радиально
   - рассчитано 10 черновых проходов
   - оценка объёма снятия: 207.345 см³

ОГРАНИЧЕНИЯ ПРОВЕРКИ
- Живой деплой Railway в локальном контейнере не выполнялся.
- Живой запрос OpenAI не нужен для ручного маркера и не выполнялся.
- Перед станком обязательны проверка координат, симуляция, Single Block и Dry Run.

ИТОГ
Сборка готова к загрузке в корень GitHub/Railway-проекта с заменой файлов.
