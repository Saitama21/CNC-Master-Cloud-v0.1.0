CNC Assistant Client Pro v3.1.0 — OpenCV + AI Contour

Добавлено:
- серверная предобработка выбранной области через OpenCV;
- удаление мелкого текста, цифр и стрелок;
- консервативное подавление диагональной штриховки;
- усиление основных линий и замыкание небольших разрывов;
- предпросмотр «исходник / очищено» до отправки в OpenAI;
- отдельные переключатели каждого фильтра;
- автоматическая отправка GPT только очищенной выбранной области;
- диагностика количества удалённых элементов;
- API /api/v1/client/opencv/preview;
- версия API и интерфейса v3.1.0.

Важно:
OpenCV не определяет реальные миллиметры по пикселям и не заменяет проверку оператора.
Если фильтр удалил часть профиля, отключите соответствующий фильтр и обновите предпросмотр.
Перед переносом в SINUMERIK обязательно подтвердите точки X/Z, Z0, режим X и выполните симуляцию.

Проверки:
- Python compileall: OK
- JavaScript syntax: OK
- Pytest: 42 passed
